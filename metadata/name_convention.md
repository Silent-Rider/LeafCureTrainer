## Naming convention

- `ff` — full-frame (photos with lab background, classification models)
- `iso` — isolated leaves (cropped by mask, black background, classification models)
- `seg` — segmentation models (U-Net)
- `v` — version
- `ft` — fine-tuned
- `lr` — learning rate